[alembic]
# Named "migrations", not "alembic": the app runs with backend/ on sys.path, and a
# local directory called alembic would shadow the installed package.
script_location = migrations
# The database location comes from the application settings; see migrations/env.py.
sqlalchemy.url =
